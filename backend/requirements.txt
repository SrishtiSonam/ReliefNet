fastapi==0.110.0
uvicorn[standard]==0.29.0
motor==3.3.2
pydantic==2.6.4
pydantic-settings==2.2.1
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.1
torch==2.2.1
scipy==1.13.0
python-dotenv==1.0.1
httpx==0.27.0


# ─── Core ─────────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
motor>=3.3.2                    # Async MongoDB driver
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ─── ML / Data Science ────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0             # B.2 — Random Forest, Ridge, StandardScaler
xgboost>=2.0.0                  # Optional: swap RF for XGBoost
torch>=2.2.0                    # B.7 — LSTM flood predictor, PPO, NN-VFA
scipy>=1.12.0                   # B.3 — used in linprog (backup solver)
joblib>=1.3.2                   # Model serialization

# ─── Optimization ─────────────────────────────────────────────────────────────
pulp>=2.8.0                     # B.3 + B.5 + B.6 — MIP solver (CBC backend)

# ─── Authentication (B.12) ────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0  # JWT signing/verification
passlib[bcrypt]>=1.7.4            # Password hashing
python-multipart>=0.0.9           # OAuth2PasswordRequestForm body parsing

# ─── PDF Export (B.12) ────────────────────────────────────────────────────────
reportlab>=4.1.0                  # PDF generation

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
bson>=0.5.10
gymnasium>=0.29.0                 # Improvement 6 — standard RL benchmark env wrapper